var elements = document.getElementsByClassName('text-danger');

var el = elements[1]; // Select the third one from the NodeList
el.className = 'text-primary'; // Change the value of its class attribute